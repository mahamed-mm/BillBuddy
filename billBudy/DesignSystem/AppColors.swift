import SwiftUI

enum AppColors {
    static let bbTeal = Color(red: 0, green: 0.898, blue: 0.8)
    static let bbBackground = Color(.systemBackground)
    static let bbCardBackground = Color(.systemGray6)
    static let bbPrimaryText = Color.primary
    static let bbSecondaryText = Color.secondary
    static let bbSelectedChip = bbTeal.opacity(0.15)
    static let bbSelectedBorder = bbTeal
    static let bbUnselectedBorder = Color(.systemGray4)
}
